version: 1.0

description: A workflow to copy hpe Nimble alarms from mongo and into snow..

tasks:
  getalerts:
    action: hpenimble.get_mongo_volumes_2_nimble
    next:
      - when: <% succeeded() %>
        publish:
          - volumes: <% result().result %>
        do: nimble

  nimble:
    with: <% ctx().volumes %>
    action: hpenimble.post_volume payload='<% item() %>'
    next:
      - when: <% succeeded() %>
        do: processnimble

  processnimble:
    action: hpenimble.process_nimble_volumes volumes=<% ctx().volumesß %>
